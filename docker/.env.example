# Copy to docker/.env and set values for your local environment.
# Never commit docker/.env or database dumps to version control.

MYSQL_ROOT_PASSWORD=change-me-root-password
MYSQL_DATABASE=tscharts
MYSQL_USER=tscharts
MYSQL_PASSWORD=change-me-app-password

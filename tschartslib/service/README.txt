The files here basically support connections to the RESTful API from the
unit test code that is located in the test directory. They (and the 
example code in the unit tests) can be used as starting points for building
clients that talk to the RESTful API.

